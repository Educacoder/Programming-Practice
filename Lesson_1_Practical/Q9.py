# Using the string (F-string) method,
# print the following lines of values in a table formatting

# Name column should be 20 characters wide and left aligned
# Age column should be 8 characters wide with value as whole number and center aligned
# Weight column should be 8 characters wide with value having one decimal place and right aligned
# Column separator is “|”